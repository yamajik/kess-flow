{
  "name": "kess-flow",
  "version": "0.1.5",
  "description": "Advance Async FBP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build:clean": "rm -rf dist",
    "build:compress": "tsc",
    "build": "npm run build:clean && npm run build:compress",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "npm run build"
  },
  "author": "me@yamajik.com",
  "license": "ISC",
  "devDependencies": {
    "@types/events": "^3.0.0",
    "@types/node": "^14.14.31",
    "@types/uuid": "^8.3.0",
    "ts-node": "^9.1.1"
  },
  "dependencies": {
    "@kess-flow/ioredis-mock": "^5.2.7",
    "@types/ioredis": "^4.22.0",
    "events": "^3.2.0",
    "ioredis": "^4.23.0",
    "typescript": "^4.2.2",
    "uuid": "^8.3.2"
  }
}
